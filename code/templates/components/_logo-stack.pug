//- Logo stack
//-
//- A group of logos, showcasing either clients or technology from our toolkit


//- Options
//- -- alt
//- -- src
//- -- description

mixin logo-stack(args)
  .logo-stack(class=attributes.class)
    each item, index in args
      if item.href
        a.logo-stack__item(href=item.href, target=item.target)
          img(src="images/logos/" + item.src, alt=item.alt)
      else
        div.logo-stack__item
          img(src="images/logos/" + item.src, alt=item.alt)
          if item.description
            div.logo-stack__description
              p=item.description
