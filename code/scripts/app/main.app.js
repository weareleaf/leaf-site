// var myDep = require('some-npm-installed-dependency');

window.app = {
  initialize: function() {
    // This is the main entry point for any application built in this project.
    console.log("Application initialized...");
  }
}

window.app.initialize();