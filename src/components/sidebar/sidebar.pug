mixin sidebar(args)
  .sidebar
    .sidebar__component
      ul.styled-list.styled-list--inside
        each item, index in args
            li.styled-list__item.styled-list__item--gap-lg!= item.item
