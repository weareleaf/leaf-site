mixin author(c)
  .author(class= c.class)
    +image({
      context: "author",
      src: c.thumbnail.src,
      alt: c.thumbnail.alt
    })
    .author__content
      h2.author__name= c.name
      p.author__details= c.details