mixin tile(c)
  .tile
    if c.image
      +image({
        context: "tile",
        src: c.image.src,
        alt: c.image.alt
      })
    .tile__content
      h3.tile__heading= c.heading
      p.tile__text= c.text
