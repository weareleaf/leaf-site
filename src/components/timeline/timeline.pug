mixin timeline(c)
  .timeline
    each item in c.timelineItems
      .timeline__item
        .timeline__marker
          img(src="/assets/images/company/marker.svg")
        .timeline__content
          +block(item)
